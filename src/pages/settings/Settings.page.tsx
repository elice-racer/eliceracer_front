import CheckedVersion from "./components/CheckedVersion";

function Settings() {
  return (
    <div>
      settings.page
      <CheckedVersion id="version" />
    </div>
  );
}

export default Settings;
